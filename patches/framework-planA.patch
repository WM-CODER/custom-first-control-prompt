diff --git a/packages/boot/app-boot/src/profile.ts b/packages/boot/app-boot/src/profile.ts
index 8f982bed80..1cfa0146d0 100644
--- a/packages/boot/app-boot/src/profile.ts
+++ b/packages/boot/app-boot/src/profile.ts
@@ -364,8 +364,10 @@ export function resolveBundleDir(
  * @param installAnchor - absolute path of the dsh app's package.json (first resolution anchor).
  * @param home - the Harness home; defaults to {@link resolveDshHome}.
  * @param options - `userLayer: false` skips reading `cordis.patch.yml`, so a
- * bundles-only consumer (`--dump-default-config`, a recovery diagnostic)
- * cannot fail on a broken user layer.
+ * bundles-only consumer (e.g. `--dump-default-config`, a recovery diagnostic)
+ * never touches the file. On the default path an existing but unparseable
+ * profile patch layer degrades to an empty patch list with a loud warning
+ * instead of failing the profile.
  * @returns the loaded profile (empty `patches` when the user layer is skipped).
  */
 export function loadProfile(
@@ -396,9 +398,21 @@ export function loadProfile(
     return { packageName, packageDir, patchPath, patches: loadOverlayPatches(binName, patchPath) }
   })
   const patchPath = join(dir, PROFILE_PATCH_FILENAME)
-  const patches = options.userLayer !== false && existsSync(patchPath)
-    ? loadOverlayPatches(binName, patchPath)
-    : []
+  let patches: PatchOptions[] = []
+  if (options.userLayer !== false && existsSync(patchPath)) {
+    try {
+      patches = loadOverlayPatches(binName, patchPath)
+    } catch (error) {
+      // The profile's own patch layer is the deployment-editable surface. A
+      // broken user layer must degrade to "no patches" with a loud warning,
+      // never fail the profile and take every command (including `web`) down
+      // with it. Bundle layers and `--patch` overlays stay fail-loud: they are
+      // owned by the deployment, and such a file is a misconfiguration.
+      const message = error instanceof Error ? error.message : String(error)
+      console.error(`${binName}: ignoring unparseable ${PROFILE_PATCH_FILENAME} at ${patchPath}: ${message}`)
+      console.error(`${binName}: this profile boots WITHOUT its user patch layer; repair the file to re-enable it or run 'dsh config dump --profile ${name}' for the composed tree`)
+    }
+  }
   return { name, dir, layers, patchPath, patches }
 }
 
diff --git a/packages/boot/app-boot/tests/profile.spec.ts b/packages/boot/app-boot/tests/profile.spec.ts
index bd0294475d..1c35a7f829 100644
--- a/packages/boot/app-boot/tests/profile.spec.ts
+++ b/packages/boot/app-boot/tests/profile.spec.ts
@@ -143,6 +143,26 @@ describe('loadProfile', () => {
     expect(bare.layers).toEqual([])
   })
 
+  it('degrades an unparseable user patch layer to no patches instead of failing the profile', () => {
+    const anchor = stageInstallation({
+      'bundle-a': { patch: '- insert:\n    - id: a\n      name: pkg-a\n' },
+    })
+    const home = tmp()
+    const dir = resolveProfileDir('demo', home)
+    initProfile(dir, ['bundle-a'])
+    // A syntactically broken file must not brick the profile (or `web`).
+    writeFileSync(join(dir, PROFILE_PATCH_FILENAME), 'invalid: [unclosed\n')
+    expect(() => loadProfile('t', 'demo', anchor, home)).not.toThrow()
+    expect(loadProfile('t', 'demo', anchor, home).patches).toEqual([])
+    // An empty or comment-only file parses to null/undefined, never an array:
+    // same degrade contract.
+    writeFileSync(join(dir, PROFILE_PATCH_FILENAME), '# only a comment\n')
+    expect(loadProfile('t', 'demo', anchor, home).patches).toEqual([])
+    // The fixed file is picked up again on the next load.
+    writeFileSync(join(dir, PROFILE_PATCH_FILENAME), '- id: a\n  config:\n    v: 3\n')
+    expect(loadProfile('t', 'demo', anchor, home).patches).toHaveLength(1)
+  })
+
   it('auto-initializes only shipped templates and fails loud otherwise', () => {
     const anchor = stageInstallation({})
     const home = tmp()
diff --git a/packages/core/agent-loop/src/index.ts b/packages/core/agent-loop/src/index.ts
index 371154a7c9..563fcc1c2d 100644
--- a/packages/core/agent-loop/src/index.ts
+++ b/packages/core/agent-loop/src/index.ts
@@ -22,7 +22,7 @@ import type {
 import { errorChain } from '@deepseek-ai/dsh-llm'
 import { installSettingsSection, settingsNamespace } from '@deepseek-ai/dsh-settings'
 import { SessionId, SessionPreparation } from '@deepseek-ai/dsh-session'
-import type { Session, SessionHeader } from '@deepseek-ai/dsh-session'
+import type { Session, SessionEvent, SessionHeader } from '@deepseek-ai/dsh-session'
 import type {} from '@deepseek-ai/dsh-system-prompt'
 import type {} from '@deepseek-ai/dsh-tools'
 import type { SessionPersistence } from '@deepseek-ai/dsh-session-persistence'
@@ -181,6 +181,34 @@ declare module '@deepseek-ai/cordis' {
      * @mode emit
      */
     'agent-loop/config-start-failed'(payload: { sessionId: SessionId; error: unknown }): void
+    /**
+     * Contribute durable seed events before a session is prepared. Listeners
+     * receive the identity and creation metadata of the session about to be
+     * created and the seed accumulated so far; call `next()` to reach
+     * downstream contributions plus the caller-supplied seed, then return the
+     * complete seed list. Contributed events must form balanced, fully closed
+     * turns contiguously numbered from the accumulated seed.
+     * @param payload.sessionId - identity of the session being created.
+     * @param payload.meta - creation metadata; mirrors `CreateAgentOptions.meta`
+     *   (cwd, fork lineage, origin, delegation budget, preset).
+     * @param next - downstream contributions plus the caller-supplied seed.
+     * @returns the complete seed event list the factory passes to the session boundary.
+     * @mode waterfall
+     */
+    'agent-loop/session-seed'(
+      payload: {
+        sessionId: SessionId
+        meta: Readonly<{
+          cwd?: string
+          parentSession?: SessionId
+          seedLength?: number
+          origin?: 'subagent'
+          delegationDepth?: number
+          agentPreset?: string
+        }> | undefined
+      },
+      next: () => Promise<readonly SessionEvent[]>,
+    ): Promise<readonly SessionEvent[]>
   }
 }
 
@@ -604,9 +632,15 @@ export class AgentLoop extends Service implements AgentFactory {
    * @returns the published handle.
    */
   async createAgent(ownerCtx: Context, options: CreateAgentOptions): Promise<AgentHandle> {
+    const seed = await this.runtime.ctx.waterfall(
+      'agent-loop/session-seed',
+      { sessionId: options.sessionId, meta: options.meta },
+      (): Promise<readonly SessionEvent[]> => Promise.resolve(options.seed ?? []),
+    )
+    const hasSeed = options.seed !== undefined || seed.length > 0
     const preparation = SessionPreparation.create(this.runtime.ctx.sessions.prepare(options.sessionId, {
-      ...options.seed === undefined ? {} : { seed: options.seed },
-      ...options.meta === undefined ? {} : { meta: options.meta },
+      ...(hasSeed ? { seed } : {}),
+      ...(options.meta === undefined ? {} : { meta: options.meta }),
     }))
     const published = this.setupAndPublish(
       ownerCtx,
diff --git a/packages/core/session/src/index.ts b/packages/core/session/src/index.ts
index 2d82a88623..29c7c590a4 100644
--- a/packages/core/session/src/index.ts
+++ b/packages/core/session/src/index.ts
@@ -328,8 +328,14 @@ function assertMessageEventShape(event: Record<string, unknown>, subject: string
   }
   const sourceRecord = source as Record<string, unknown>
   if (type === 'assistant/message') {
-    if (sourceRecord['kind'] !== 'model' || !hasProviderModel(sourceRecord)) {
-      throw new Error(`${subject} message must have model source`)
+    if (sourceRecord['kind'] === 'model') {
+      if (!hasProviderModel(sourceRecord)) {
+        throw new Error(`${subject} message must have model source with provider/model`)
+      }
+    } else if (sourceRecord['kind'] !== 'plugin'
+      || typeof sourceRecord['plugin'] !== 'string'
+      || sourceRecord['plugin'] === '') {
+      throw new Error(`${subject} message must have model or plugin source`)
     }
     return
   }
diff --git a/packages/core/session/tests/session.spec.ts b/packages/core/session/tests/session.spec.ts
index 47453ea6b5..e6396bd5f6 100644
--- a/packages/core/session/tests/session.spec.ts
+++ b/packages/core/session/tests/session.spec.ts
@@ -251,7 +251,7 @@ describe('Session', () => {
             message: { ...assistant, source: { kind: 'user' } },
           },
         },
-        message: 'message must have model source',
+        message: 'message must have model or plugin source',
       },
       {
         name: 'tool source',
