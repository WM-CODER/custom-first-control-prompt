/**
 * Client-namespace projection of the panel wire types: a pure re-export of the
 * package's panel-types outlet. Client code imports ONLY the client namespace,
 * so `./client` projects the same single-source content the host serves.
 *
 * @module @wm-coder/dsh-custom-first-control-prompt/client
 */
export {};
//# sourceMappingURL=client.js.map