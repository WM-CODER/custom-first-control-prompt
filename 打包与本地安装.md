# 打包与本地安装（通用流程）

> 本文档描述如何将 `@wm-coder/dsh-custom-first-control-prompt` 插件从源码构建并安装到本地 DSH 部署（不经过 npm 发布）。
> 文中所有路径均为占位符，请替换为你自己的实际路径。
>
> 功能说明见同目录 [README.md](README.md) / [README.zh.md](README.zh.md)。

## 0. 术语与路径约定

| 名称 | 路径（占位符） |
|---|---|
| 构建工厂（harness 仓库内副本） | `<repo>/packages/context/custom-first-control-prompt` |
| 分发目录（本插件产品仓库） | `<folder>`（本文件夹；含 `lib/`、`client-ui/`、脚本与文档） |
| 部署安装位置 | `<deploy-root>/.dsh/profiles/web/node_modules/@wm-coder/dsh-custom-first-control-prompt` |
| 部署组合配置 | `<deploy-root>/.dsh/profiles/web/cordis.patch.yml` |

> **双名体系（重要）**：构建工厂内的包保持 `@deepseek-ai/dsh-*` 工厂名（harness 仓库的
> workspace 约束强制 release member 的 repository 指向官方仓库）；分发目录的产品包名是
> `@wm-coder/dsh-*`。**产物镜像后必须做包名字符串重写**（见 1.2 步骤 3），与 sourcemap
> 本机路径清洗同一模式。
>
> 注意：**分发目录不能独立构建**。它的 `tsconfig.json` 通过 `extends` 依赖仓库根配置，
> `references` 指向仓库内其它包，且 `tsc`/`tsdown` 工具链位于仓库的 pnpm workspace 中。
> 所有构建必须在 harness 仓库目录内执行。

## 1. 打包（构建 lib 产物）

### 1.1 前置条件

- Node.js ≥ 22（构建本身无强版本要求，但部署运行时用到了 `node:zlib` 的 zstd，需 22+）。
- 仓库依赖已安装（`<repo>/node_modules` 存在）。
- 构建工具：`tsc`（TypeScript 编译器）+ `tsdown`（rolldown 打包器），均为仓库 devDependencies。

### 1.2 构建流水线（四步：改源 → 根构建 → 镜像+重写 → 门禁）

**步骤 1 — 源码同步**：源码只在工厂目录（`<repo>/packages/context/custom-first-control-prompt/src`、
`<repo>/packages/client/ui-custom-first-control-prompt/src`）修改；构建后把 `src/`、`tests/`
同步到分发目录（分发目录的源码保持与工厂一致，import 用工厂包名——分发目录不构建）。

**步骤 2 — 根构建**（在**仓库根**执行，不要在包目录单独跑 tsdown——见已知坑）：

```powershell
cd <repo>
npx tsc -b tsconfig.host.json
npx tsdown --env.DSH_BUILD_FACE host     # workspace 模式 + typertPlugin（typert 工件 + 装饰器 lowering）
npx tsdown --env.DSH_BUILD_FACE client   # 面板浏览器 bundle（client-ui/lib/client.js）
```

**步骤 3 — 镜像 + 包名重写 + sourcemap 清洗**：把
`packages/context/custom-first-control-prompt/lib/` 与
`packages/client/ui-custom-first-control-prompt/lib/` 整目录镜像到本文件夹的
`lib/` 与 `client-ui/lib/`（排除 `tsconfig.tsbuildinfo`），然后：
- 删除未被 `index.js`/`invariant.js` 引用的 stale `seed-*.js` chunk；
- **包名重写**：产物内所有 `@deepseek-ai/dsh-custom-first-control-prompt` 与
  `@deepseek-ai/dsh-client-ui-custom-first-control-prompt` 字符串替换为 `@wm-coder/*`
  （运行时 `remote` 导入、d.ts 类型声明、patch 行渲染字符串，约 21 个 js/d.ts）；
- **sourcemap 清洗**：`client-ui/lib/client.js` 的 `dsh-css:` 标记内嵌构建机绝对路径，
  替换为仓库相对路径。
（以上三步可脚本化；重写/清洗脚本在工厂仓库侧维护，不在分发目录内。）

**步骤 4 — 门禁**：跑 `verify-build.ps1`（语法 / 无裸 `@Remote(` / 导入冒烟 /
注入实现内联 / 无 stale chunk / typert 工件）+ 全仓敏感信息扫描
（用户名、盘符绝对路径、AppData 等 0 命中）。

> **历史命令（已废弃，勿用）**：早期文档写「包目录内 `pnpm exec tsc -b --force` +
> `pnpm exec tsdown`」。该方式在 workspace 模式下绕过根配置的装饰器 lowering，
> 会产出**裸 `@Remote(`** 的 `lib/index.js`（Node 无法解析，web 起不来），
> 且不生成 typert 工件。包级 `tsdown.config.ts` 已删除以防误用；
> 根因记录见 DEBUG-NOTES.zh.md §0.2 与 §1.1。

### 1.3 产物说明

根 workspace 构建（host face）输出到 `lib/`：

```
lib\index.js               入口插件（name/inject/apply/Config + 注入监听器；含装饰器
                           lowering 与内联的 buildSeedMessages，自包含无 chunk 依赖）
lib\invariant.js           空伴生（满足构建工厂的测试伴生制度；请求路径零日志写入，
                           无可校验对象，产品部署不引用）
lib\typert.host.js         Typert Host 生成产物（Remote 方法表；运行时依赖 zod）
lib\typert.remote-client.js Typert Client 生成产物（面板 Remote 调用半）
lib\index.d.ts             类型声明（位于 lib/ 根）
lib\types\*.js / *.d.ts    tsc 增量产物（tsdown 的输入）
lib\*.map                  构建调试产物（发布时由 files 白名单排除）
```

- C-only bundle 自包含：`index.js` 内联种子构建与注入逻辑，**不再产生 `seed-*.js`
  chunk**（旧版本有；若镜像后见到未被引用的 seed chunk 即为 stale，删除）。
- `package.json` 的 `exports` 不暴露 `./invariant`（产品部署不需要空伴生）。
- `lib/` 下的 `*.map` 是构建调试产物，不随包发布（`package.json` 的 `files` 白名单已排除）。

### 1.4 已知坑

1. **沙箱/权限**：构建需要写仓库目录。若在受限文件沙箱下运行，`tsdown` 会报 `os error 5 / EACCES 拒绝访问`（`tsc` 因增量跳过而"假成功"）。此时需要对构建命令授予宽权限。
2. **构建环境**：`tsconfig.json` 依赖仓库根配置与 references，必须在仓库根执行
   host/client face 构建（见 1.2）；**不要**在包目录单独跑 `pnpm exec tsdown`——
   会绕过装饰器 lowering 产出裸 `@Remote(` 坏产物且缺 typert 工件
   （verify-build.ps1 门禁 #2/#5 会拦）。分发目录无法构建。
3. **stale chunk 堆积**：根 tsdown `clean: false`，旧 `seed-<hash>.js` 会跨构建堆积；
   每次镜像前删除未被 `index.js`/`invariant.js` 引用的 chunk（门禁 #4b 会拦）。
4. **sourcemap 本机路径**：`client-ui/lib/client.js` 的 `dsh-css:` sourcemap 标记会内嵌
   构建机绝对路径；对外分发前必须清洗为仓库相对路径（重建后需重新清洗）。
5. **包名双体系**：工厂名 `@deepseek-ai/*` 会随产物带出（import 字符串、d.ts 声明、
   patch 行渲染）；镜像后不做重写会让部署解析失败（junction 目录是 `@wm-coder` scope）。

## 2. 本地安装（不经 npm）

> **当前推荐**走 [INSTALL.md](INSTALL.md) 的两种方式：
> - **方式 A（官方）**：`dsh plugin --profile web add <folder> <folder>/client-ui`
>   （link 安装 + bundle 层自动激活，要求 dsh `next` 线 0.1.0-rc.6）；
> - **方式 B（离线/研发机）**：profile 注册目录下建 junction + profile patch 自带两行，
>   `install.ps1 -Offline` 一键完成。
>
> 此处保留 junction 手工流程用于理解解析原理。

### 2.1 创建部署包目录与 junction

```powershell
$dst = '<deploy-root>\.dsh\profiles\web\node_modules\@wm-coder\dsh-custom-first-control-prompt'
New-Item -ItemType Directory -Path (Split-Path $dst) -Force
New-Item -ItemType Junction -Path $dst -Target '<folder>'
```

> 部署的 `node_modules` 是 pnpm 布局，但**手动放入一个 `@wm-coder` 作用域目录**即可被
> Node 解析（`import('@wm-coder/dsh-custom-first-control-prompt')` 从该目录向上查找
> `@deepseek-ai/schemastery`、`@deepseek-ai/cordis` 等框架依赖，全部命中部署已有的包）。
> 不要用 `Copy-Item -Recurse` 直接复制整个 `node_modules`（会跟随 pnpm 符号链接导致大量错误）。

### 2.2 验证安装

在 `<deploy-root>\.dsh\profiles\web` 下执行（Node 沿目录向上解析到 `profiles\node_modules`）：

```powershell
node -e "import('@wm-coder/dsh-custom-first-control-prompt').then(m => console.log(m.name, JSON.stringify(m.inject), typeof m.apply, typeof m.Config))"
# 期望输出：custom-first-control-prompt ["systemPrompt","llm","sessions"] function function
```

## 3. 组合机制与自定义配置

### 3.1 bundle 层（方式 A 自动激活）

核心包声明了 `"dsh": {"bundle": {"patch": "./cordis.patch.yml"}}`；包内
`cordis.patch.yml` 以 `- insert:` 插入**核心行（name=自己，含中性示例默认配置）+
面板行（name=client-ui 包）**。`dsh plugin add` 的对账把它追加进 profile 的
`dsh.profile.bundles`，组合时该层位于官方 bundle 之后、profile patch 之前。
方式 B（junction）没有对账，profile patch 必须自带同样的两行（install.ps1 代写）。

### 3.2 自定义配置：带 id 的定向覆盖（不要复制 insert 行）

编辑 `<deploy-root>\.dsh\profiles\web\cordis.patch.yml`，追加**带 id 的非 insert
patch**（覆盖 bundle 行 config，后写胜出），样例即本仓库 `cordis.patch.yml.template`：

```yaml
- id: custom-first-control-prompt
  config:
    sections:
      - name: system
        order: -50
        text: '这是测试提示词'
    history:
      - user: '用户测试提示词1'
        assistant: '助手提示词1'
      - user: '用户测试提示词2'
        assistant: '助手提示词2'
    includeSubagents: false
```

> 示例文本用**中性测试内容**（`用户测试提示词1`/`助手提示词1` 这种）即可，不要用
> "每条回复必须以句号结尾"之类的**自指规则**——规则性样例会让模型在测试/验证时陷入
> 规则复读甚至死循环。

- patch 语义（源码实锤）：不带 id 的 `insert` 追加到根 entry 列表；**带 id 的非
  insert patch 按 key 覆盖目标 entry 的 config/disabled**（`cordis-plugin-include`）。
- 配置契约（name/order/enabled/text/history/includeSubagents）见 README。
- **重复 id 警告**：bundle 层已带两行后，profile patch 里再 `- insert:` 同 id 行
  （旧手工安装的残留）= 根列表重复 → web fail-loud 起不来；`uninstall.ps1` 会
  外科式剥离这两种残留行（保留其它条目与注释）。
  详见 [INSTALL.md](INSTALL.md) 1.3 与 [DEBUG-NOTES.zh.md](DEBUG-NOTES.zh.md) §1.3。

### 3.2 验证组合解析（离线）

```powershell
node '<deploy-root>\.dsh\profiles\node_modules\@deepseek-ai\dsh\lib\bin.js' --profile web --dump-config
# 输出中应出现 - id: custom-first-control-prompt 及其配置
```

### 3.3 生效机制

部署通过 `watchUserPatches`（`dsh-app-boot`）用 Cordis HMR 监听 patch 文件，修改后**事务性热重载**：
- `sections`：立即对**所有会话**的后续请求生效（每轮组装系统提示词时渲染）。
- `history`：对**所有后续普通对话请求**生效（请求路径注入；配置变更 HMR 后新请求即带新种子）。
- 插件**源码/产物**变更必须重启 web（Node ESM 模块缓存，目录更新 ≠ 进程生效）。

### 3.4 运行时验证

- 决定性验证用面板「LLM 监听」：开监听 → 发消息 → 请求列表中的普通对话请求
  （无 `[purpose]` 徽标）点开后前几条即注入的种子消息。
- 直接观察：新发一条消息，问模型「你看到了几条参考对话」，能复述配置内容即注入成功。
- 日志验证（负向）：会话日志**不应**有任何 plugin 来源的 user/message 事件（零日志写入是特性）。

## 4. 卸载 / 回滚

1. **临时停用**：profile patch 写 `- id: custom-first-control-prompt` + `disabled: true`
   的定向覆盖，HMR 自动撤销。
2. **正式卸载**（方式 A 装的）：`dsh plugin --profile web remove
   @wm-coder/dsh-custom-first-control-prompt @wm-coder/dsh-client-ui-custom-first-control-prompt`
   —— pnpm 移除两个 link 依赖，对账同步移除 bundle 层。
3. **一键清理**（两种模式通吃）：`uninstall.ps1` = 官方 remove + 双 scope junction
   清理（兼容旧 `@deepseek-ai` 与新 `@wm-coder` 历史安装）+ profile patch 里本插件
   行的外科式剥离（保留其它条目与注释）。手动删 junction：
   ```powershell
   [System.IO.Directory]::Delete('<deploy-root>\.dsh\profiles\web\node_modules\@wm-coder\dsh-custom-first-control-prompt', $false)
   ```

## 5. 发布状态备忘

- 该包当前**未发布到 npm**，`publishConfig.access: public` 已就绪；正式发布需 npm
  `@wm-coder` 作用域账号。
- 版本号：`0.2.0`（与 `package.json` 一致；C-only 收敛为破坏性变更：移除
  `seedMode`/`historyMode`/`reapplyAfterCompaction` 配置键）。
- 许可证：MIT（见 `LICENSE`）；仓库地址 `WM-CODER/custom-first-control-prompt`。

## 6. 单文件夹安装（给 AI 的口令式安装）

本文件夹自包含「核心插件 + 管理面板 + 安装手册」，用户只需把文件夹交给 AI 说「安装」：

- `INSTALL.md` —— AI 安装手册（官方 `dsh plugin add` 主线 / junction 离线回退 + 验证步骤）。
- `cordis.patch.yml` —— 核心包的 bundle 层（`dsh.bundle` 声明指向它；核心行 + 面板行 +
  中性示例默认配置，`dsh plugin add` 对账自动激活）。
- `cordis.patch.yml.template` —— 用户层覆盖样例（带 id 的定向 patch，非 insert；
  复制 insert 行会与 bundle 层撞 id 让 web 起不来，模板注释有说明）。
- `client-ui/` —— **管理面板正式客户端包**（`@wm-coder/dsh-client-ui-custom-first-control-prompt`），
  浏览器 bundle 已构建，**持久存在**，web 重启后无需重装。功能：对话输入框上方可折叠的
  「LLM 请求监听」dock（`conversation.input.dock`，经 `llm/stream` 采集真实明文请求，
  完整请求列表带 `[purpose]` 徽标，默认暂停，支持开始/停止/清空/隐藏）、设置页
  「自定义优先控制提示词」区（预览注入段与参考历史 / 配置编辑 sections+history+
  includeSubagents / RAW 导入导出 / LLM 请求控制台）、设置→插件卡片（dock 可见性与监听开关）。
- `DEBUG-NOTES.zh.md` —— 调试与排障笔记 + 测试方法（fail-loud 根因、API 验证链路等）。
- `INSTALL-FULL.zh.md` —— 跨机完整实测流程（v3）。
