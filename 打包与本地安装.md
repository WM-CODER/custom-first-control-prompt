# 打包与本地安装（通用流程）

> 本文档描述如何将 `@deepseek-ai/dsh-custom-first-control-prompt` 插件从源码构建并安装到本地 DSH 部署（不经过 npm 发布）。
> 文中所有路径均为占位符，请替换为你自己的实际路径。
>
> 功能说明见同目录 [README.md](README.md) / [README.zh.md](README.zh.md)。

## 0. 术语与路径约定

| 名称 | 路径（占位符） |
|---|---|
| 源码/构建（仓库） | `<repo>/packages/context/custom-first-control-prompt` |
| 工作区副本 | `<workspace>/custom-first-control-prompt` |
| 部署安装位置 | `<deploy-root>/.dsh/profiles/node_modules/@deepseek-ai/dsh-custom-first-control-prompt` |
| 部署组合配置 | `<deploy-root>/.dsh/profiles/web/cordis.patch.yml` |
| 会话日志（验证用） | `<deploy-root>/.dsh/sessions/<workspace-dir>/<session-id>/session.jsonl.zstd` |

> 注意：**工作区副本不能独立构建**。它的 `tsconfig.json` 通过 `extends: "../../../tsconfig.base.json"` 依赖仓库根配置，`references` 指向仓库内其它包（vendor/cordis、core/agent 等），且 `tsc`/`tsdown` 工具链位于仓库的 pnpm workspace 中。所有构建必须在仓库目录内执行。

## 1. 打包（构建 lib 产物）

### 1.1 前置条件

- Node.js ≥ 22（构建本身无强版本要求，但部署运行时用到了 `node:zlib` 的 zstd，需 22+）。
- 仓库依赖已安装（`<repo>/node_modules` 存在）。
- 构建工具：`tsc`（TypeScript 编译器）+ `tsdown`（rolldown 打包器），均为仓库 devDependencies。

### 1.2 构建命令

在仓库包目录下执行（二选一）：

```powershell
# 方式 A：增量构建（源码未变时 tsc 可能跳过）
cd <repo>\packages\context\custom-first-control-prompt
pnpm exec tsc -b
pnpm exec tsdown

# 方式 B：强制全量重建（推荐，确保类型声明与 JS 产物都最新）
cd <repo>\packages\context\custom-first-control-prompt
pnpm exec tsc -b --force
pnpm exec tsdown
```

### 1.3 产物说明

`tsdown` 输出到 `lib/`（构建日志示例）：

```
lib\index.js               入口插件（name/inject/apply/Config/validate）
lib\invariant.js           运行时不变式伴生（seeded 消息形状校验）
lib\seed-<hash>.js         代码分割产物（renderTranscript / hasSeededHistory /
                           buildHistoryMessage / seedTranscript / 框架常量）
lib\index.d.ts / invariant.d.ts
                           类型声明（新布局，位于 lib/ 根）
lib\*.map                  构建调试产物（发布时由 files 白名单排除）
```

- 哈希文件名（如 `seed-<hash>.js`）随源码内容变化：**看到哈希变了，说明源码修改已进入产物**。
- `package.json` 的 `types`/`exports` 已指向新布局 `./lib/index.d.ts` 与 `./lib/invariant.d.ts`，安装后无需再修正。
- `lib/` 下的 `*.map` 是构建调试产物，不随包发布（`package.json` 的 `files` 白名单已排除）。

### 1.4 已知坑

1. **沙箱/权限**：构建需要写仓库目录。若在受限文件沙箱下运行，`tsdown` 会报 `os error 5 / EACCES 拒绝访问`（`tsc` 因增量跳过而"假成功"）。此时需要对构建命令授予宽权限。
2. **构建环境**：`tsconfig.json` 依赖仓库根配置与 references，必须在仓库内（如 `packages/context/custom-first-control-prompt`）执行 `pnpm exec tsc -b --force` + `pnpm exec tsdown`；工作区独立副本无法构建。

## 2. 本地安装（不经 npm）

> ⚠️ 本节是早期的手工拷贝流程，仅作历史参考。**当前推荐**走
> [INSTALL.md](INSTALL.md) 里的两种实机验证过的安装方式：
> - **方式 A（官方）**：`dsh plugin --profile web add <folder> <folder>/client-ui`
>   （link 安装，要求 dsh `next` 线 0.1.0-rc.6）；
> - **方式 B（离线/研发机，本文档所在机器实际使用）**：profile 注册目录下建 junction，
>   `install.ps1` 一键完成（junction + 补丁写入 + 备份）。
>
> 此处保留手工流程用于理解解析原理（Node 从 `profiles/node_modules` 向上解析依赖）。

### 2.1 创建部署包目录

```powershell
$dst = '<deploy-root>\.dsh\profiles\node_modules\@deepseek-ai\dsh-custom-first-control-prompt'
New-Item -ItemType Directory -Path $dst -Force
```

> 部署的 `node_modules` 是 pnpm 布局，但**手动放入一个 `@deepseek-ai` 作用域目录**即可被 Node 解析（`import('@deepseek-ai/dsh-custom-first-control-prompt')` 从该目录向上查找 `@deepseek-ai/schemastery`、`@deepseek-ai/cordis` 等依赖，全部命中部署已有的包）。不要用 `Copy-Item -Recurse` 直接复制整个 `node_modules`（会跟随 pnpm 符号链接导致大量错误）。

### 2.2 复制文件

用文件级复制（避免 `-Recurse` 遍历 pnpm 链接结构）：

```powershell
$src = '<repo>\packages\context\custom-first-control-prompt'
# package.json + README
Copy-Item (Join-Path $src 'package.json') $dst
Copy-Item (Join-Path $src 'README.md'), (Join-Path $src 'README.zh.md'), (Join-Path $src 'README.i18n.yaml') $dst
# lib 产物（js + d.ts；map 无需复制）
$libDst = Join-Path $dst 'lib'
New-Item -ItemType Directory -Path $libDst -Force
Get-ChildItem (Join-Path $src 'lib') -File | Where-Object { $_.Extension -in '.js', '.d.ts' } | ForEach-Object { Copy-Item $_.FullName $libDst -Force }
```

### 2.3 验证安装（types 已指向新布局，无需修正）

`package.json` 的 `types`/`exports` 已指向新布局（`./lib/index.d.ts`、`./lib/invariant.d.ts`），复制后无需再修正。在 `<deploy-root>\.dsh\profiles\web` 下执行（Node 沿目录向上解析到 `profiles\node_modules`）：

```powershell
node -e "import('@deepseek-ai/dsh-custom-first-control-prompt').then(m => console.log(m.name, JSON.stringify(m.inject), typeof m.apply, typeof m.Config))"
# 期望输出：custom-first-control-prompt ["agents","systemPrompt"] function function
```

## 3. 配置启用（组合 patch）

### 3.1 编辑 `<deploy-root>\.dsh\profiles\web\cordis.patch.yml`

该文件初始为 `[]`（空 patch 层），组合由 bundles（`dsh-base`、`dsh-web-app`）+ patch 层叠加而成。追加：

```yaml
- insert:
    - id: custom-first-control-prompt
      name: '@deepseek-ai/dsh-custom-first-control-prompt'
      config:
        sections:
          - name: system
            order: -50
            text: '这是测试提示词'
        history:
          - user: '用户测试提示词1'
            assistant: '助手提示词1'
          - user: '用户测试提示词2'
            assistant: '助手提示词2'
        includeSubagents: false
        historyMode: 'session-start'
```

> 示例文本用**中性测试内容**（`用户测试提示词1`/`助手提示词1` 这种）即可，不要用
> "每条回复必须以句号结尾"之类的**自指规则**——规则性样例会让模型在测试/验证时陷入
> 规则复读甚至死循环。与 `profile-web.patch.yml`（实测部署截图）保持一致。

- `insert` 不带 `id` → 直接追加到根 entry 列表（`cordis-plugin-include` 的 patch 语义）。
- 配置契约（name/order/enabled/text/history/includeSubagents/historyMode）见 README。
- **重复 id 警告（实机踩过的坑）**：profile patch 默认写**核心插件行 + 面板行**两行
  （npm 0.1.x 发布物 bundle 不带面板行，必须写在 profile）。**仅当**部署 bundle 确认
  自带面板行（从含该行的主线构建的 web-app）时删掉面板行——同 id 重复 insert 会让
  web 起不来（fail-loud）。核心行本身也只应出现一次（bundle 已自带则删掉 profile 条目）。
  详见 [INSTALL.md](INSTALL.md) 1.3 与 [DEBUG-NOTES.zh.md](DEBUG-NOTES.zh.md) §1.3。

### 3.2 验证组合解析（离线）

```powershell
node '<deploy-root>\.dsh\profiles\node_modules\@deepseek-ai\dsh\lib\bin.js' --profile web --dump-config
# 输出中应出现 - id: custom-first-control-prompt 及其配置
```

### 3.3 生效机制（无需重启）

部署通过 `watchUserPatches`（`dsh-app-boot`）用 Cordis HMR 监听 patch 文件，修改后**事务性热重载**：
- `sections`：立即对**所有会话**的后续请求生效（每轮组装系统提示词时渲染）。
- `history`：只对**新会话**生效（`agent/session-start` 时注入一次；resume/fork 不重复，配置变更不重写已有会话）。

### 3.4 运行时验证

- 直接观察：新发一条消息，若系统提示词注入段生效，回复应符合规则。
- 日志验证：解压会话日志（`node:zlib` 的 `zstdDecompressSync` + 按 zstd frame magic `0xFD2FB528` 扫描 frame 边界），检查 `request/header` 事件的系统提示词是否包含注入文本。时间线对照：配置写入**之前**的请求不含注入段，写入**之后**的请求包含。

## 4. 卸载 / 回滚

1. **停用插件**：从 `cordis.patch.yml` 删除该 `insert` 段（或给 entry 加 `disabled: true`），HMR 自动撤销。
2. **移除包**：
   ```powershell
   Remove-Item '<deploy-root>\.dsh\profiles\node_modules\@deepseek-ai\dsh-custom-first-control-prompt' -Recurse -Force
   ```

## 5. 发布状态备忘

- 该包当前**未发布到 npm**，`publishConfig.access: public` 已就绪。
- 若需正式发布：修正 `package.json` 的 `types`/`exports` 路径后，在仓库内 `pnpm --filter @deepseek-ai/dsh-custom-first-control-prompt publish`（需 npm 登录与 @deepseek-ai 作用域权限）。
- 版本号：`0.1.0-rc.6`（与 `package.json` 一致；分发包钉版 `next` 线）。

## 6. 单文件夹安装（给 AI 的口令式安装）

本文件夹自包含「核心插件 + 管理面板 + 安装手册」，用户只需把文件夹交给 AI 说「安装」：

- `INSTALL.md` —— AI 安装手册（官方 `dsh plugin add` 或 junction 安装 + 补丁行 + 验证步骤）。
- `cordis.patch.yml.template` —— 补丁行模板（**只含核心行**，面板行默认由 bundle 提供；
  重复写面板行会让 web 起不来，模板注释有说明）。
- `client-ui/` —— **管理面板正式客户端包**（`@deepseek-ai/dsh-client-ui-custom-first-control-prompt`），
  浏览器 bundle 已构建，**持久存在**，web 重启后无需重装。功能：对话输入框上方可折叠的
  「LLM 请求监听」dock（`conversation.input.dock`，经 `llm/stream` 采集真实明文请求，
  默认暂停，支持开始/停止/清空/隐藏）、设置页「自定义优先控制提示词」区（预览注入段与
  参考历史 / 配置编辑 sections+history+historyMode+includeSubagents / RAW 导入导出 /
  LLM 请求控制台）、设置→插件卡片（dock 可见性与监听开关）。
- `panel/` —— 历史进程内动态面板（`cordis_define` 时代）的占位，仅作参考；正式面板在
  `client-ui/`，无需它。
- `DEBUG-NOTES.zh.md` —— 调试与排障笔记 + 测试方法（fail-loud 根因、API 验证链路等）。

> 早期版本的面板是**进程内动态插件**，web 重启后需重新定义；现已升级为正式客户端包，
> **持久无需重装**。
